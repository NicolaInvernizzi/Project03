using UnityEngine;

public class CameraMove : MonoBehaviour
{
    //public float speed = 10f;
    //float xPosition;
    //Vector3 pos;

    //void Update()
    //{
    //    MoveCamera();
    //}
    //private void MoveCamera()
    //{
    //    xPosition = Input.GetAxis("Mouse 0") * speed * Time.deltaTime;
    //    xPosition = Input.GetAxis("Mouse 1") * speed * Time.deltaTime;
    //    pos = new Vector3(x, transform.position.y, transform.position.z);
    //    transform.position = 
    //}
}
